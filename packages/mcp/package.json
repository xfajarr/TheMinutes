{
  "name": "@theminutes/mcp",
  "version": "0.0.1",
  "private": true,
  "bin": {
    "theminutes-mcp": "./src/server.ts"
  },
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun build src/server.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@theminutes/sdk": "workspace:*",
    "@theminutes/types": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
