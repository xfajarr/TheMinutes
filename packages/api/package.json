{
  "name": "@theminutes/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "@theminutes/proxy": "workspace:*",
    "@theminutes/router": "workspace:*",
    "@theminutes/types": "workspace:*",
    "hono": "^4.12.8",
    "ioredis": "^5.3.2",
    "nanoid": "^5.0.7",
    "postgres": "^3.4.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "5.9.2",
    "vitest": "^2.0.0"
  }
}
